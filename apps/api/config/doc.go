// Package config loads and exposes application configuration from environment
// variables. The top-level [Config] struct is populated via the
// github.com/caarlos0/env library and covers application settings and
// PostgreSQL connection parameters.
package config
