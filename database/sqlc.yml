version: "2"
sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./queries"
    gen:
      go:
        package: "repository"
        out: "../internal/repository"
        emit_interface: true
