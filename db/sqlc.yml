version: "2"
sql:
  - engine: "postgresql"
    schema: "./migrations"
    queries: "./queries"
    gen:
      go:
        package: "sqlc"
        out: "../api/internal/platform/postgres/sqlc"
        emit_interface: true
